import 'package:flutter/material.dart';

const int flexSizeMain = 7;
const int flexSizeSide = 2;

const primaryColor = Color(0xFFFFC107);
const secondaryColor = Color(0xFF242430);
const darkColor = Color(0xFF191923);
const bodyTextColor = Color(0xFF8B8B8D);
const bgColor = Color(0xFF1E1E28);
const colorBlack = Colors.black;
const colorWhite = Colors.white;

const defaultPadding = 20.0;

const defaultDurationFast = Duration(milliseconds: 500);
const defaultDurationMedium = Duration(milliseconds: 1000);
const defaultDurationSlow = Duration(milliseconds: 1500);
